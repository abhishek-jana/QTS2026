# Issue tracker

This project tracks issues using GitHub Issues.

When a skill tells you to read, create, or update issues, use the `gh` CLI (e.g. `gh issue view`, `gh issue list`, `gh issue create`, `gh issue edit`). Always pass `--json` and use `jq` to parse the output reliably when reading issues.
