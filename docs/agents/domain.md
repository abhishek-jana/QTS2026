# Domain docs

This is a single-context repository.

When a skill tells you to learn the domain or check for architectural decisions:

1. Read `CONTEXT.md` at the repo root to learn the domain language, core entities, and invariant rules. Always use this vocabulary in your output.
2. Search `docs/adr/` at the repo root for past architectural decisions that might constrain your current task.
